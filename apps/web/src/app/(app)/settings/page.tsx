import { Wip } from "@/components/app/wip";

export const metadata = { title: "Settings" };

export default function SettingsPage() {
  return (
    <Wip
      title="Settings"
      note="Business profile, subscription plan and account preferences will be managed here."
    />
  );
}
